agent:
  max_turns: 150
browser:
  backend: none
display:
  tool_progress: "off"
  platforms:
    whatsapp:
      tool_progress: "off"
      live_status: "off"
approvals:
  mode: "off"
gateway:
  multiplex_profiles: false
